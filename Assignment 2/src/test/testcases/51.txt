
		Function: main
		Body:
			Var: x;
			x = int_of_string(read());
			y = int_of_string(read());
			z = int_of_string(read());
			If (x > 5) Then
				print(y);
			ElseIf (x < 3) Then 
				**missing expr**
			EndIf.
		EndBody.
		